<div class="row">
  <div class="col">
    <% flash.each do |name, msg| %>
      <% if name == 'notice' %>
      	<% name = 'primary' %>
      <% else %>
      	<% name = 'danger' %>
      <% end %>

      <div class="alert alert-<%= name %>">
        <a href="#" class="close" data-dismiss="alert">×</a>
        <%= msg %>
      </div>
    <% end %>
  </div>
</div>