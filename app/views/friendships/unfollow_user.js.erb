$('#unfollow-button').text('Follow')
                     .attr('class', 'btn follow-button btn-outline-primary')
                     .attr('href', "/<%= @user.id %>/follow_user")
                     .attr('id', 'follow-button');
                     